document.querySelectorAll('a[href^="#"]').forEach(link=>{link.addEventListener('click',event=>{const target=document.querySelector(link.getAttribute('href'));if(target){event.preventDefault();target.scrollIntoView({behavior:'smooth'});}})});const form=document.querySelector('[data-coach-form]');if(form){form.addEventListener('submit',event=>{event.preventDefault();const message=document.querySelector('[data-form-message]');message.hidden=false;message.textContent='Thank you. Your information has been received for this preview. A live connection service will be added before launch.';form.reset();});}